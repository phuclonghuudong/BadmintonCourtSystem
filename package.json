{
  "name": "badminton_courts_system",
  "version": "1.0.0",
  "description": "backend",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client-install": "cd client && npm install",
    "install": "concurrently \"npm run client-install\"",
    "client": "cd client && npm run dev",
    "start": "concurrently \"npm run client\""
  },
  "dependencies": {
    "concurrently": "^9.1.2"
  }
}
